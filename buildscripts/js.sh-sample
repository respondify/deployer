#!/bin/bash

# Build script for JS. First argument ($1) is the full path to the deploy directory.

if [[ $# -ne 1 ]]; then
  echo "Deploy folder not specified" >&2
  exit 2
fi

#pushd $1 > /dev/null 2>&1
#echo "Building js"
#echo "PROCESSING" >> testa.js
#popd > /dev/null 2>&1
